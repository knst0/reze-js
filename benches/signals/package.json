{
  "name": "@rezejs/bench-signals",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench --run && node ./scripts/merge.mjs"
  },
  "dependencies": {
    "@rezejs/signals": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  }
}
